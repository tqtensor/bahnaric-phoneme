{
    "name": "Python Project",
    "build": {
        "dockerfile": "Dockerfile",
        "context": "..",
        "args": {
            // Add any build arguments you need here.
        }
    },
    "runArgs": [
        // Add any runtime arguments you need here.
    ],
    "customizations": {
        "vscode": {
            "settings": {
                "terminal.integrated.shell.linux": "/bin/bash"
            },
            "extensions": [
                "ms-python.python"
            ]
        }
    },
    "forwardPorts": [],
    "postCreateCommand": "pip install -r requirements.txt"
}